<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>

    <!-- NuGet パッケージ情報 -->
    <Version>0.1.0-alpha.1</Version>
    <PackageId>Ateliers.Voice.Engines.VoicePeakTools</PackageId>
    <Authors>ateliers</Authors>
    <Company>ateliers.dev</Company>
    <Product>Ateliers Voice Engines</Product>
    <Description>VOICEPEAK CLI を使用した音声生成エンジン</Description>
    <PackageTags>voicepeak;voice;tts;speech;synthesis;audio;wav;</PackageTags>
    <PackageProjectUrl>https://github.com/yuu-git/ateliers-voice-engines</PackageProjectUrl>
    <RepositoryUrl>https://github.com/yuu-git/ateliers-voice-engines</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <!-- Assembly情報 -->
    <AssemblyVersion>0.1.0.0</AssemblyVersion>
    <FileVersion>0.1.0.0</FileVersion>
    <InformationalVersion>0.1.0-alpha.1</InformationalVersion>

    <!-- NuGet パッケージ設定 -->
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <!-- README を NuGet パッケージに含める -->
  <ItemGroup>
    <None Include=".\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Ateliers.Voice.Engines\Ateliers.Voice.Engines.csproj" />
  </ItemGroup>

</Project>
